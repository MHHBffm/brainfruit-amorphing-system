---
import VentureLayout from '../components/VentureLayout.astro';
import Logo from '../components/Logo.astro';
import { getVentureWithStyle, getAllVenturesWithStyles } from '../utilities/venture';

const ventureKey = 'amorphing';
const { venture, style } = getVentureWithStyle(ventureKey);
const allVentures = getAllVenturesWithStyles();
---

<VentureLayout
	ventureKey={ventureKey}
	title="Amorphing · Responsive Brand Design"
	description="AI-native brand systems that morph across ventures, languages, and contexts. Built with parametric design, SSOT principles, and radical transparency."
	lang="en"
>
	<main>
		<!-- Hero Section -->
		<section class="hero">
			<div class="logo-container">
				<Logo ventureKey={ventureKey} showName size="medium" />
			</div>

			<p class="tagline">Responsive Brand Design</p>
			<p class="description">{venture.description}</p>
		</section>

		<!-- The 5 Pillars -->
		<section class="pillars">
			<h2>The 5 Pillars of Amorphic Design</h2>

			<div class="pillar-grid">
				<article class="pillar">
					<h3>1. Data Transparency</h3>
					<p>Every design decision is traceable to source data (LAB, RAL, WCAG). No magic, no guessing.</p>
				</article>

				<article class="pillar">
					<h3>2. Parametric Everything</h3>
					<p>One input generates infinite variants. Colors, logos, layouts morph from shared DNA.</p>
				</article>

				<article class="pillar">
					<h3>3. No Decoration</h3>
					<p>Form follows function. Geometric primitives (circles, lines, grids). Beauty emerges from constraints.</p>
				</article>

				<article class="pillar">
					<h3>4. Accessibility First</h3>
					<p>WCAG AAA by default. Design systems that work for everyone, everywhere.</p>
				</article>

				<article class="pillar">
					<h3>5. Modular DNA</h3>
					<p>Ventures inherit core traits, express unique phenotypes. One OS, infinite organisms.</p>
				</article>
			</div>
		</section>

		<!-- Showcase: 4 Ventures -->
		<section class="ventures">
			<h2>Built with Amorphing</h2>
			<p class="ventures-desc">Four ventures, one design DNA. Watch as brand identity morphs across contexts:</p>

			<div class="venture-grid">
				{allVentures.slice(0, 4).map((v) => (
					<div class="venture-card" style={`--venture-color: ${v.style.primary};`}>
						<Logo ventureKey={v.key} size="small" className="venture-logo-component" />
						<h3>{v.venture.name}</h3>
						<p>{v.venture.description}</p>
					</div>
				))}
			</div>
		</section>

		<!-- CTA -->
		<section class="cta">
			<h2>Ready to morph your brand?</h2>
			<p>Amorphing is in private beta. Request early access:</p>
			<a href="mailto:hello@amorphing.com" class="cta-button">hello@amorphing.com</a>
		</section>

		<!-- Footer -->
		<footer>
			<p>© 2026 Amorphing · Part of <a href="https://brainfruit.de">brainfruit</a> · Built with <a href="https://astro.build">Astro</a></p>
			<p class="footer-meta">Open Source · AI-Native · WCAG AAA</p>
		</footer>
	</main>

	<style>
		/* Page uses CSS Variables from ventures-theme.css */
		/* All venture-specific colors automatically applied via data-venture attribute */

		:root {
			--spacing: 8px;
		}

		* {
			margin: 0;
			padding: 0;
			box-sizing: border-box;
		}

		body {
			font-family: var(--body-font);
			line-height: 1.6;
			color: var(--accent-color);
			background: #f9fafb;
		}

		main {
			max-width: 1200px;
			margin: 0 auto;
			padding: calc(var(--spacing) * 4);
		}

		/* Hero Section */
		.hero {
			text-align: center;
			padding: calc(var(--spacing) * 12) 0 calc(var(--spacing) * 8);
		}

		.logo-container {
			display: flex;
			align-items: center;
			justify-content: center;
			gap: calc(var(--spacing) * 3);
			margin-bottom: calc(var(--spacing) * 4);
		}

		.logo {
			width: 120px;
			height: 120px;
			filter: drop-shadow(0 4px 16px rgba(139, 92, 246, 0.3));
		}

		.venture-name {
			font-family: var(--heading-font);
			font-size: 4rem;
			font-weight: 600;
			color: var(--primary-color);
			margin: 0;
		}

		.tagline {
			font-size: 1.5rem;
			font-weight: 500;
			color: var(--secondary-color);
			margin-bottom: calc(var(--spacing) * 2);
		}

		.description {
			font-size: 1.125rem;
			color: var(--secondary-color);
			max-width: 700px;
			margin: 0 auto;
		}

		/* Pillars Section */
		.pillars {
			padding: calc(var(--spacing) * 8) 0;
			background: white;
			border-radius: calc(var(--spacing) * 2);
			margin-bottom: calc(var(--spacing) * 6);
		}

		.pillars h2 {
			font-family: var(--heading-font);
			font-size: 2.5rem;
			text-align: center;
			margin-bottom: calc(var(--spacing) * 6);
			color: var(--accent-color);
		}

		.pillar-grid {
			display: grid;
			grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
			gap: calc(var(--spacing) * 4);
			padding: 0 calc(var(--spacing) * 4);
		}

		.pillar {
			padding: calc(var(--spacing) * 3);
			border-left: 4px solid var(--primary-color);
		}

		.pillar h3 {
			font-family: var(--heading-font);
			font-size: 1.25rem;
			margin-bottom: calc(var(--spacing) * 2);
			color: var(--primary-color);
		}

		.pillar p {
			color: var(--secondary-color);
			line-height: 1.7;
		}

		/* Ventures Section */
		.ventures {
			padding: calc(var(--spacing) * 8) 0;
		}

		.ventures h2 {
			font-family: var(--heading-font);
			font-size: 2.5rem;
			text-align: center;
			margin-bottom: calc(var(--spacing) * 2);
			color: var(--accent-color);
		}

		.ventures-desc {
			text-align: center;
			font-size: 1.125rem;
			color: var(--secondary-color);
			margin-bottom: calc(var(--spacing) * 6);
		}

		.venture-grid {
			display: grid;
			grid-template-columns: repeat(auto-fit, minmax(240px, 1fr));
			gap: calc(var(--spacing) * 4);
		}

		.venture-card {
			background: white;
			border-radius: calc(var(--spacing) * 2);
			padding: calc(var(--spacing) * 4);
			text-align: center;
			border: 2px solid #e5e7eb;
			transition: all 0.3s ease;
		}

		.venture-card:hover {
			border-color: var(--venture-color);
			box-shadow: 0 8px 24px rgba(0, 0, 0, 0.1);
			transform: translateY(-4px);
		}

		.venture-logo-component {
			margin-bottom: calc(var(--spacing) * 2);
		}

		.venture-card h3 {
			font-family: var(--heading-font);
			font-size: 1.5rem;
			margin-bottom: calc(var(--spacing));
			color: var(--accent-color);
		}

		.venture-card p {
			color: var(--secondary-color);
			font-size: 0.875rem;
		}

		/* CTA Section */
		.cta {
			text-align: center;
			padding: calc(var(--spacing) * 10) calc(var(--spacing) * 4);
			background: var(--primary-gradient);
			border-radius: calc(var(--spacing) * 2);
			color: white;
			margin-bottom: calc(var(--spacing) * 6);
		}

		.cta h2 {
			font-family: var(--heading-font);
			font-size: 2.5rem;
			margin-bottom: calc(var(--spacing) * 2);
		}

		.cta p {
			font-size: 1.25rem;
			margin-bottom: calc(var(--spacing) * 4);
			opacity: 0.95;
		}

		.cta-button {
			display: inline-block;
			padding: calc(var(--spacing) * 2) calc(var(--spacing) * 4);
			background: white;
			color: var(--primary-color);
			text-decoration: none;
			border-radius: calc(var(--spacing));
			font-weight: 600;
			font-size: 1.125rem;
			transition: all 0.3s ease;
		}

		.cta-button:hover {
			background: #f3f4f6;
			transform: scale(1.05);
		}

		/* Footer */
		footer {
			text-align: center;
			padding: calc(var(--spacing) * 4) 0;
			color: var(--secondary-color);
			border-top: 1px solid #e5e7eb;
		}

		footer p {
			margin-bottom: calc(var(--spacing));
		}

		footer a {
			color: var(--primary-color);
			text-decoration: none;
		}

		footer a:hover {
			text-decoration: underline;
		}

		.footer-meta {
			font-size: 0.875rem;
			color: var(--secondary-color);
		}

		/* Responsive */
		@media (max-width: 768px) {
			.venture-name {
				font-size: 2.5rem;
			}

			.logo {
				width: 80px;
				height: 80px;
			}

			.pillars h2,
			.ventures h2,
			.cta h2 {
				font-size: 2rem;
			}

			.pillar-grid,
			.venture-grid {
				grid-template-columns: 1fr;
			}
		}
	</style>
</VentureLayout>
